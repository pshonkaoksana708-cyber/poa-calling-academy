# PRODUCTION READY REPORT

Дата проверки: 29 июля 2026

## Объём проверки

Проверена текущая готовность сайта POA CALLING — Академии профессионального развития к production-запуску без изменения дизайна, структуры страниц, навигации и учебного контента.

## Главная страница

Проверенные блоки:

- первый экран;
- кнопки hero;
- карточки профессий;
- блоки преимуществ;
- блок сертификата;
- блок автора;
- форма заявки;
- FAQ;
- footer.

Статус:

- блоки присутствуют;
- пустых блоков по коду не обнаружено;
- форма отображает обязательный email и чекбокс согласия;
- кнопка отправки отключена без согласия;
- после submit показывается локальное сообщение;
- внешняя отправка формы и оплата не подключены.

Форма дополнительно проверена в браузере:

- `email` required: да;
- чекбокс согласия required: да;
- кнопка disabled до согласия: да;
- кнопка активируется после согласия: да;
- после submit появляется сообщение: да;
- console errors: 0.

## Страницы профессий

Проверены:

- `/profession/supply`
- `/profession/hr`
- `/profession/tourism`
- `/profession/logistics`
- `/profession/ai`

Статус:

- страницы существуют;
- изображения профессий и уровней ссылаются на существующие файлы;
- кнопки ведут на существующие anchors или публичные страницы;
- блок сертификата подключён;
- пустых критичных блоков по маршрутам не обнаружено.

## SEO

Проверено:

- `title`;
- `description`;
- Open Graph;
- favicon;
- `robots.txt`;
- `sitemap.xml`.

Статус:

- metadata настроены в `app/layout.tsx`;
- Open Graph image: `/images/hero/academy-og.jpg`;
- favicon: `/icon.svg`;
- robots: `app/robots.ts`;
- sitemap: `app/sitemap.ts`;
- production-домен не захардкожен;
- URL строится через `NEXT_PUBLIC_SITE_URL` или `SITE_URL`.

Рекомендация для production-домена:

- после выбора домена задать `NEXT_PUBLIC_SITE_URL=https://ваш-домен`;
- при необходимости продублировать `SITE_URL=https://ваш-домен`;
- затем проверить абсолютные URL в `/sitemap.xml` и `Sitemap` в `/robots.txt`.

## Контактные данные

Найденный email:

- `info@cpr.education`

Где используется:

- footer;
- юридические страницы;
- служебные страницы доступа/ошибки;
- email config.

Телефоны:

- публичный контактный телефон не найден;
- поле телефона есть в форме заявки, но не является обязательным.

Статус:

- данные не менялись;
- перед публикацией владельцу нужно подтвердить email и юридические формулировки.

## Env

Создан отдельный файл:

- `PRODUCTION_ENV_CHECKLIST.md`

Ключевые переменные:

- `NEXT_PUBLIC_SITE_URL`;
- `SITE_URL`;
- `ACCESS_TOKEN_SECRET`;
- `NODE_ENV`.

Статус:

- `.env*` файлы в корне проекта не найдены;
- `ACCESS_TOKEN_SECRET` нужно обязательно задать в production;
- fallback `development-only-change-this-access-token-secret` допустим только для разработки.

## Формы

Проверена форма заявки `components/AccessRequestForm.tsx`.

Текущая логика:

- `email` обязателен;
- чекбокс согласия обязателен;
- кнопка отключена, пока согласие не принято;
- submit не отправляет данные на сервер;
- после submit выводится сообщение о принятом согласии и следующем этапе оплаты.

Статус:

- форма готова как интерфейс MVP;
- для production-продаж нужно подключить backend, CRM, email или платёжный сервис.

## Изображения

Проверено 33 ссылки на `/images/...`.

Результат:

- отсутствующих файлов нет;
- сертификатный шаблон существует;
- Open Graph image существует;
- изображения профессий и уровней существуют.

## Проверки

### TypeScript

Команда:

```bash
tsc --noEmit
```

Фактический запуск в текущем окружении:

```bash
./node_modules/.bin/tsc --noEmit
```

Результат: успешно.

### ESLint

Команда в проекте:

```bash
npm run lint
```

Фактический запуск в текущем окружении:

```bash
eslint .
```

Результат: успешно.

Примечание: в текущей Codex-среде `npm` не установлен в PATH. Точная команда `npm run lint` возвращает `command not found: npm`. При этом `lint` script добавлен в `package.json`, а сама проверка ESLint прошла напрямую через локальный бинарь.

### Build

Команда в проекте:

```bash
npm run build
```

Фактический запуск в текущем окружении:

```bash
next build
```

Результат: успешно.

Примечание: точная команда `npm run build` в текущей Codex-среде возвращает `command not found: npm`. Production build проверен напрямую через локальный бинарь Next.js.

### Route audit

Проверены 87 маршрутов: публичные маршруты, страницы профессий, страницы программ, уроки, аттестация, сертификат и 404.

Результат:

- проблем: 0;
- критичных 404/500 не обнаружено;
- отсутствующих изображений не обнаружено.

### Responsive smoke

Проверены:

- `/`
- `/profession/supply`
- `/profession/hr`
- `/profession/tourism`
- `/profession/logistics`
- `/profession/ai`

Ширины:

- desktop: 1440 px;
- tablet: 768 px;
- mobile: 390 px.

Результат:

- горизонтальный overflow: нет;
- битые изображения: нет;
- console errors: 0.

## ГОТОВО

- Образовательные страницы и маршруты собраны.
- SEO-слой подготовлен под env-домен.
- Robots и sitemap существуют.
- Сертификатные страницы существуют.
- Юридические страницы существуют.
- TypeScript, ESLint и production build проходят.

## ТРЕБУЕТ ПОДКЛЮЧЕНИЯ

- Production-домен.
- `NEXT_PUBLIC_SITE_URL` / `SITE_URL`.
- `ACCESS_TOKEN_SECRET`.
- Почта для отправки доступа.
- Платёжный провайдер.
- Backend/CRM/webhook для заявок.
- Реальная выдача token-доступа после оплаты.

## ПЕРЕД ПУБЛИКАЦИЕЙ

- Подтвердить email `info@cpr.education`.
- Подтвердить юридические страницы.
- Решить, оставлять ли демо-сертификат `DEMO-2026-000001` публично на первом релизе.
- После подключения домена повторно проверить `/robots.txt`, `/sitemap.xml`, Open Graph и все публичные маршруты уже на production URL.
